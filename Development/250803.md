-engine
-wrapper
-newbauth
-statebauth
-manager
-_stub, opcode - spi는 sdk가 쓰는거니까 고정
1. 초음파
- power
  - prepare 부터
  - cleanup에 끔

2. 정전식
- power
  - prepare
  - enroll
  - identify
  - request

3. 광학식
- power
  - prepare
  


- power 가 켜져야하는 이유
  - 초음파 : MCU, interrupt, spi
  - 정전식 : interrupt, spi
  - 광학식 : spi

- general utils에 newbauth의 상태를 전달한다면?
  - stateAuth-Init-opcode : 비동기는 가능한가?
  - 이 상태를 power가 보고 있다면?
  - 다시말해 코드 곳곳에 power on off가 있는게 아니라 power 객체가 저 상태를 보고 켜고끄는걸 감지하는 거지.
  - 그러면 구간이 보인다.
  - 그런데 세부 컨트롤이 가능할까?
  - stateAuth-Init-4 
    - 초음파 on
    - 정전식 on
    - 광학식 off
  - stateAuth-Init-5
    - 초음파 on
    - 정전식 on
    - 광학식 on
  - stateAuth-Init-6
    - 

```cpp
struct poweractivezone{
start;
end;
poweractivezone(start, end);
};
```
## 초음파
### power
  - start : Prepare
  - end : CleanUp

## 광학식
### power
1. 인증
- start : IdentifyDo, opcode = 5  
  end : IdentifyFinal, opcode = 0

2. 등록
- start : EnrollDo, opcode = 5  
  end : EnrollFinal, opcode = 0

3. prepare
- start : prepare  ,???
  end : prepare, ???

4. getsensorstatus
- start : gensensorstatus, ???  
  end : getsensorstatus, ???

### spi

1. 인증

- start : IdentifyDo, opcode = 5  
  end :IdentifyDo, opcode = 0

- start : IdentifyFinal, opcode = 7  
  end : IdentifyFinal, opcode = 0
  
2. 등록
- start : EnrollDo, opcode = 5  
  end : EnrollDo, opcode = 0

- start : EnrollFinal, opcode = 7  
  end : EnrollFinal, opcode = 0
  
3. cbge
- start : Cbge, opcode = generatecbge  
  end : Cbge, opcode = 


---
Q. opcode 하나에서 끝날땐?
- opcode 생성자
- opcode 소멸자

Q. spi 사용하는 opcode/stub 를 전부 알아야한다.

Q. 끄는게 중요하다. 잘못된 끔은 동작중이던 것을 종료시킬 수 있다.

Q. power 가 켜져있을때만 spi가 되게하려면
```cpp
spiOn(){
  if(Off == power){
    log("error");
  }
  power.spion
}
```